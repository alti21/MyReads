import React from 'react';
import SearchBar from '../components/SearchBar';

const SearchPage = () => {
  return (
    <div>
      <SearchBar type="text" placeholder="Search for a book" />
    </div>
  );
};

export default SearchPage;
